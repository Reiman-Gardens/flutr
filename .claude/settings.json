{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const f=(process.env.TOOL_INPUT_FILE_PATH||'').replace(/\\\\\\\\/g,'/'); if(f.match(/\\.env/) || f.endsWith('docker-compose.yml')) { console.error('Protected file: '+f); process.exit(2); }\""
          }
        ]
      }
    ]
  }
}
